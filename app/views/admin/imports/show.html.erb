<h1 class="my-3"><%= @import.applied? ? "Changes" : "Preview changes" %></h1>

<%= render "imports/preview",
  import: @import,
  updates: @updates,
    apply_path: @import.applied? ? nil : apply_admin_import_path(@import),
    apply_method: :get %>
